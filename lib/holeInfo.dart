class holeInfo {
  int holeCount = 18;
  bool newRound = true;
  DateTime date = new DateTime.now();
  String roundName = 'Untitled';
  List<int> par = List.filled(18, 0);
  List<int> score = List.filled(18, 0);
}
